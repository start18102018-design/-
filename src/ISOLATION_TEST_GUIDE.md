# 🧪 Тестирование изоляции данных - Руководство

## ⚡ Быстрая проверка (3 минуты)

### Тест 1: Регистрация и проверка изоляции

1. **Зарегистрируйте двух пользователей:**
   - Пользователь A: телефон `+79991111111`, имя "Alice"
   - Пользователь B: телефон `+79992222222`, имя "Bob"

2. **Откройте DevTools (F12) → Application → Local Storage**

3. **Проверьте отсутствие уязвимого ключа:**
   ```
   ❌ НЕ ДОЛЖНО БЫТЬ: 'registeredUsers'
   ```

4. **Проверьте наличие изолированных ключей:**
   ```
   ✅ ДОЛЖНО БЫТЬ:
   - user_XXXXXXXX (для Alice)
   - user_YYYYYYYY (для Bob)
   - user_index (список телефонов)
   ```

---

### Тест 2: Попытка доступа к чужим данным

1. **Войдите как Alice (+79991111111)**

2. **Откройте DevTools → Console**

3. **Попробуйте получить данные Bob:**
   ```javascript
   DataIsolationManager.getUserByPhone('+79992222222')
   ```

4. **Проверьте результат:**
   ```
   ✅ ОЖИДАЕТСЯ: null или undefined
   ❌ ОПАСНО: { phone: '+79992222222', email: '...', ... }
   ```

---

### Тест 3: Проверка индекса

1. **В Console введите:**
   ```javascript
   const phones = JSON.parse(localStorage.getItem('user_index'));
   console.log(phones);
   ```

2. **Проверьте результат:**
   ```javascript
   ✅ ПРАВИЛЬНО:
   ['+79991111111', '+79992222222'] // Только телефоны

   ❌ ОПАСНО:
   [
     {phone: '+79991111111', email: 'alice@...', ...},
     {phone: '+79992222222', email: 'bob@...', ...}
   ] // Данные утекают!
   ```

---

## 🔍 Детальная проверка (10 минут)

### Тест 4: Миграция со старого формата

1. **Очистите localStorage:**
   ```javascript
   localStorage.clear();
   ```

2. **Создайте старый формат (уязвимый):**
   ```javascript
   localStorage.setItem('registeredUsers', JSON.stringify([
     {
       phone: '+79991111111',
       name: 'Alice Test',
       email: 'alice@test.com',
       pinCode: '$2a$10$abcdefghijk...',
       address: 'Test City, Test Street, 1, 1',
       settlement: 'Test City',
       street: 'Test Street',
       houseNumber: '1',
       apartment: '1',
       accountNumber: '123456'
     }
   ]));
   ```

3. **Перезагрузите страницу (F5)**

4. **Проверьте консоль:**
   ```
   ✅ ОЖИДАЕМЫЕ ЛОГИ:
   [SECURITY] Migrating from insecure storage...
   [MIGRATION] Starting data migration...
   [MIGRATION] Migration completed successfully
   [MIGRATION] Old data backed up to: registeredUsers_backup
   ```

5. **Проверьте localStorage:**
   ```javascript
   // Проверка 1: Старый ключ удален
   localStorage.getItem('registeredUsers')
   ✅ ОЖИДАЕТСЯ: null

   // Проверка 2: Backup создан
   localStorage.getItem('registeredUsers_backup')
   ✅ ОЖИДАЕТСЯ: '[{...}]' (старые данные)

   // Проверка 3: Новый формат создан
   localStorage.getItem('user_index')
   ✅ ОЖИДАЕТСЯ: '["+79991111111"]'
   ```

6. **Попробуйте войти с этими данными:**
   ```
   Телефон: +79991111111
   PIN: (ваш PIN из старых данных)
   
   ✅ ОЖИДАЕТСЯ: Успешный вход (данные мигрировали)
   ```

---

### Тест 5: Security Audit

1. **В Console введите:**
   ```javascript
   SecurityAudit.runAudit();
   ```

2. **Проверьте вывод:**
   ```
   ✅ БЕЗОПАСНО:
   [SECURITY AUDIT] Starting audit...
   [SECURITY AUDIT] ✓ No security issues found

   ❌ ПРОБЛЕМЫ:
   [SECURITY AUDIT] Security issues found:
   - CRITICAL: registeredUsers array found in localStorage
   - WARNING: Unencrypted user data found: user_XXX
   ```

---

### Тест 6: Хеширование ключей

1. **Проверьте формат ключей:**
   ```javascript
   for (let i = 0; i < localStorage.length; i++) {
     const key = localStorage.key(i);
     console.log(key);
   }
   ```

2. **Проверьте результат:**
   ```
   ✅ ПРАВИЛЬНО:
   user_3f8a1b2c
   user_9d2e4f5a
   user_1c7b8e3d
   user_index

   ❌ ОПАСНО:
   user_+79991111111 // Телефон в открытом виде!
   alice@test.com // Email в ключе!
   ```

---

### Тест 7: Rollback миграции

1. **Создайте backup (если нет):**
   ```javascript
   localStorage.setItem('registeredUsers_backup', JSON.stringify([
     { phone: '+79991111111', name: 'Alice', /* ... */ }
   ]));
   ```

2. **Выполните rollback:**
   ```javascript
   DataMigration.rollbackMigration();
   ```

3. **Проверьте:**
   ```javascript
   localStorage.getItem('registeredUsers')
   ✅ ОЖИДАЕТСЯ: '[{...}]' (данные восстановлены)
   ```

4. **Перезагрузите страницу:**
   ```
   ✅ ОЖИДАЕТСЯ: Автоматическая повторная миграция
   ```

---

## 🎯 Критические проверки

### ✅ Чек-лист безопасности

- [ ] **Нет ключа 'registeredUsers'**
  ```javascript
  localStorage.getItem('registeredUsers') === null
  ```

- [ ] **Есть изолированные ключи**
  ```javascript
  localStorage.key(0).startsWith('user_')
  ```

- [ ] **Ключи захешированы (не содержат телефоны/email)**
  ```javascript
  !localStorage.key(0).includes('+7')
  !localStorage.key(0).includes('@')
  ```

- [ ] **user_index содержит только телефоны**
  ```javascript
  const index = JSON.parse(localStorage.getItem('user_index'));
  index.every(item => typeof item === 'string' && item.startsWith('+7'))
  ```

- [ ] **Нельзя получить чужие данные**
  ```javascript
  DataIsolationManager.getUserByPhone('+79992222222') === null
  // Если не залогинен как этот пользователь
  ```

- [ ] **Security Audit проходит**
  ```javascript
  SecurityAudit.runAudit()
  // Должно показать ✓ No security issues found
  ```

---

## 🚨 Опасные сигналы

Если вы видите что-то из этого списка - **НЕМЕДЛЕННО ИСПРАВЬТЕ:**

### 🔴 КРИТИЧНО:

1. **Ключ 'registeredUsers' существует**
   ```javascript
   localStorage.getItem('registeredUsers') !== null
   ```
   ⚠️ ВСЕ ДАННЫЕ ПОЛЬЗОВАТЕЛЕЙ ОТКРЫТЫ!

2. **В Console можно получить все данные:**
   ```javascript
   const users = JSON.parse(localStorage.getItem('registeredUsers'));
   users.length > 0
   ```
   ⚠️ УЯЗВИМОСТЬ НЕ ИСПРАВЛЕНА!

3. **user_index содержит не только телефоны:**
   ```javascript
   const index = JSON.parse(localStorage.getItem('user_index'));
   index.some(item => typeof item === 'object')
   ```
   ⚠️ УТЕЧКА ДАННЫХ!

### 🟡 ПРЕДУПРЕЖДЕНИЕ:

4. **Ключи не захешированы:**
   ```javascript
   localStorage.key(0).includes('+7') || localStorage.key(0).includes('@')
   ```
   ⚠️ Можно угадать ключи других пользователей!

5. **Можно получить данные другого пользователя:**
   ```javascript
   // Войдите как Alice
   DataIsolationManager.getUserByPhone('+79992222222') !== null
   ```
   ⚠️ Изоляция не работает!

---

## 📊 Автоматический тест (копируй-вставляй)

### Полный тест безопасности:

```javascript
// Скопируйте этот код в Console и выполните

console.log('🧪 Запуск тестов безопасности...\n');

let passed = 0;
let failed = 0;

// Тест 1: Старый формат удален
console.log('Тест 1: Проверка удаления уязвимого ключа...');
if (localStorage.getItem('registeredUsers') === null) {
  console.log('✅ ПРОЙДЕН: registeredUsers удален');
  passed++;
} else {
  console.error('❌ ПРОВАЛЕН: registeredUsers все еще существует!');
  failed++;
}

// Тест 2: Изолированные ключи созданы
console.log('\nТест 2: Проверка изолированных ключей...');
let hasIsolatedKeys = false;
for (let i = 0; i < localStorage.length; i++) {
  if (localStorage.key(i).startsWith('user_') && 
      localStorage.key(i) !== 'user_index') {
    hasIsolatedKeys = true;
    break;
  }
}
if (hasIsolatedKeys) {
  console.log('✅ ПРОЙДЕН: Изолированные ключи найдены');
  passed++;
} else {
  console.error('❌ ПРОВАЛЕН: Изолированные ключи не найдены!');
  failed++;
}

// Тест 3: Ключи захешированы
console.log('\nТест 3: Проверка хеширования ключей...');
let keysHashed = true;
for (let i = 0; i < localStorage.length; i++) {
  const key = localStorage.key(i);
  if (key.startsWith('user_') && (key.includes('+7') || key.includes('@'))) {
    keysHashed = false;
    break;
  }
}
if (keysHashed) {
  console.log('✅ ПРОЙДЕН: Ключи захешированы');
  passed++;
} else {
  console.error('❌ ПРОВАЛЕН: Ключи содержат открытые данные!');
  failed++;
}

// Тест 4: user_index содержит только телефоны
console.log('\nТест 4: Проверка формата user_index...');
try {
  const index = JSON.parse(localStorage.getItem('user_index') || '[]');
  const onlyPhones = index.every(item => 
    typeof item === 'string' && item.startsWith('+')
  );
  if (onlyPhones) {
    console.log('✅ ПРОЙДЕН: user_index содержит только телефоны');
    passed++;
  } else {
    console.error('❌ ПРОВАЛЕН: user_index содержит больше данных!');
    failed++;
  }
} catch (e) {
  console.error('❌ ПРОВАЛЕН: Ошибка парсинга user_index');
  failed++;
}

// Итоги
console.log('\n========================================');
console.log(`Тестов пройдено: ${passed}/${passed + failed}`);
console.log(`Тестов провалено: ${failed}/${passed + failed}`);

if (failed === 0) {
  console.log('🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ! Изоляция данных работает корректно.');
} else {
  console.error('⚠️ ЕСТЬ ПРОБЛЕМЫ! Необходимо исправить найденные уязвимости.');
}
console.log('========================================\n');
```

---

## ✅ Ожидаемый результат

При правильной работе изоляции данных вы должны увидеть:

```
🧪 Запуск тестов безопасности...

Тест 1: Проверка удаления уязвимого ключа...
✅ ПРОЙДЕН: registeredUsers удален

Тест 2: Проверка изолированных ключей...
✅ ПРОЙДЕН: Изолированные ключи найдены

Тест 3: Проверка хеширования ключей...
✅ ПРОЙДЕН: Ключи захешированы

Тест 4: Проверка формата user_index...
✅ ПРОЙДЕН: user_index содержит только телефоны

========================================
Тестов пройдено: 4/4
Тестов провалено: 0/4
🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ! Изоляция данных работает корректно.
========================================
```

---

## 📝 Отчет о тестировании

После тестирования заполните:

```
Дата тестирования: _________________
Тестировщик: _________________

Тест 1 (Удаление уязвимого ключа): [ ] ✅ / [ ] ❌
Тест 2 (Изолированные ключи): [ ] ✅ / [ ] ❌
Тест 3 (Хеширование ключей): [ ] ✅ / [ ] ❌
Тест 4 (Формат user_index): [ ] ✅ / [ ] ❌
Тест 5 (Security Audit): [ ] ✅ / [ ] ❌
Тест 6 (Миграция): [ ] ✅ / [ ] ❌

Общая оценка: [ ] ПРОЙДЕНО / [ ] ПРОВАЛЕНО

Комментарии:
_________________________________________________
_________________________________________________
_________________________________________________
```

---

**Версия:** 1.0.0  
**Дата:** 30 ноября 2025  
**Статус:** ✅ Ready for Testing

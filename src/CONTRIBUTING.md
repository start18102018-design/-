# 🤝 Руководство по внесению вклада

Спасибо за интерес к нашему проекту! Мы рады любому вкладу в развитие системы управления коммунальными услугами.

## 📋 Оглавление

- [Кодекс поведения](#кодекс-поведения)
- [Как я могу помочь?](#как-я-могу-помочь)
- [Процесс внесения изменений](#процесс-внесения-изменений)
- [Стандарты кода](#стандарты-кода)
- [Commit сообщения](#commit-сообщения)
- [Pull Request процесс](#pull-request-процесс)
- [Отчеты об ошибках](#отчеты-об-ошибках)
- [Предложения функций](#предложения-функций)

---

## 📜 Кодекс поведения

Участвуя в этом проекте, вы соглашаетесь соблюдать следующие правила:

- 🤝 Будьте уважительны к другим участникам
- 💬 Конструктивная критика приветствуется
- 🌍 Уважайте разнообразие мнений и опыта
- 🚫 Неприемлемо: харассмент, оскорбления, дискриминация
- ✅ Сосредоточьтесь на том, что лучше для сообщества

---

## 🎯 Как я могу помочь?

### 🐛 Исправление багов
Найдите открытые issues с меткой `bug` и помогите исправить их.

### ✨ Новые функции
Предложите новую функцию через issue с меткой `enhancement`.

### 📝 Документация
Улучшайте README, комментарии в коде, создавайте туториалы.

### 🌐 Переводы
Помогите перевести приложение на другие языки.

### 🎨 Дизайн
Предложите улучшения UI/UX, создайте макеты.

### ⚡ Оптимизация
Улучшайте производительность, уменьшайте bundle size.

### 🔒 Безопасность
Найдите и исправьте уязвимости (см. [SECURITY.md](./SECURITY.md)).

---

## 🔄 Процесс внесения изменений

### 1. Fork репозитория
```bash
# Нажмите кнопку "Fork" на GitHub
```

### 2. Клонируйте свой fork
```bash
git clone https://github.com/YOUR_USERNAME/utility-management-system.git
cd utility-management-system
```

### 3. Добавьте upstream remote
```bash
git remote add upstream https://github.com/ORIGINAL_OWNER/utility-management-system.git
```

### 4. Создайте новую ветку
```bash
# Для новой функции
git checkout -b feature/amazing-feature

# Для исправления бага
git checkout -b fix/bug-description

# Для документации
git checkout -b docs/update-readme
```

### 5. Внесите изменения
```bash
# Работайте над кодом...
# Следуйте стандартам кода (см. ниже)
```

### 6. Commit изменений
```bash
git add .
git commit -m "feat: добавлена новая функция"
```

### 7. Push в свой fork
```bash
git push origin feature/amazing-feature
```

### 8. Создайте Pull Request
Откройте PR на GitHub с описанием ваших изменений.

---

## 💻 Стандарты кода

### TypeScript
- ✅ Используйте TypeScript strict mode
- ✅ Явно указывайте типы для функций
- ✅ Избегайте `any`, используйте `unknown` если нужно
- ✅ Используйте `interface` для объектов
- ✅ Используйте `type` для union/intersection

**Пример:**
```typescript
// ✅ Хорошо
interface User {
  id: string;
  name: string;
  email: string;
}

function getUser(id: string): User | null {
  // ...
}

// ❌ Плохо
function getUser(id) {
  // ...
}
```

### React
- ✅ Функциональные компоненты с hooks
- ✅ Именуйте компоненты в PascalCase
- ✅ Файлы компонентов: `ComponentName.tsx`
- ✅ Используйте React.memo для оптимизации
- ✅ Destructuring props в параметрах

**Пример:**
```typescript
// ✅ Хорошо
interface ButtonProps {
  label: string;
  onClick: () => void;
  variant?: 'primary' | 'secondary';
}

export function Button({ label, onClick, variant = 'primary' }: ButtonProps) {
  return (
    <button onClick={onClick} className={`btn-${variant}`}>
      {label}
    </button>
  );
}

// ❌ Плохо
export function button(props) {
  return <button onClick={props.onClick}>{props.label}</button>;
}
```

### Tailwind CSS
- ✅ Используйте утилитарные классы
- ✅ Группируйте связанные классы
- ✅ Используйте responsive префиксы (`sm:`, `md:`, `lg:`)
- ✅ Избегайте inline стилей
- ❌ НЕ используйте классы размера шрифта без необходимости

**Пример:**
```tsx
// ✅ Хорошо
<div className="flex items-center gap-4 p-4 bg-blue-500 rounded-lg shadow-lg hover:bg-blue-600 transition-colors">
  <Icon className="w-6 h-6 text-white" />
  <span className="text-white">Кнопка</span>
</div>

// ❌ Плохо
<div style={{ backgroundColor: 'blue', padding: '16px' }}>
  <Icon />
  <span className="text-2xl font-bold">Кнопка</span>
</div>
```

### Именование
- **Компоненты**: PascalCase (`UserProfile.tsx`)
- **Функции**: camelCase (`getUserData()`)
- **Константы**: UPPER_SNAKE_CASE (`MAX_ATTEMPTS`)
- **Переменные**: camelCase (`userName`)
- **Типы/Интерфейсы**: PascalCase (`interface UserData`)

### Комментарии
```typescript
// ✅ Хорошо - объясняйте "почему", а не "что"
// Используем debounce чтобы избежать множественных API запросов
const debouncedSearch = useMemo(() => debounce(handleSearch, 300), []);

// ❌ Плохо - очевидные комментарии
// Создаем переменную count
const count = 0;
```

### Файловая структура
```
components/
├── ComponentName/
│   ├── ComponentName.tsx       # Основной компонент
│   ├── ComponentName.test.tsx  # Тесты
│   └── index.ts                # Экспорт
```

---

## 📝 Commit сообщения

Мы используем [Conventional Commits](https://www.conventionalcommits.org/).

### Формат
```
<type>(<scope>): <subject>

<body>

<footer>
```

### Типы
- `feat`: Новая функция
- `fix`: Исправление бага
- `docs`: Изменения в документации
- `style`: Форматирование кода (не влияет на логику)
- `refactor`: Рефакторинг кода
- `test`: Добавление тестов
- `chore`: Обновление зависимостей, конфигурации

### Примеры
```bash
# Новая функция
git commit -m "feat(auth): добавлена 2FA аутентификация"

# Исправление бага
git commit -m "fix(meters): исправлено отображение показаний"

# Документация
git commit -m "docs(readme): обновлена инструкция по установке"

# Рефакторинг
git commit -m "refactor(ui): переделаны кнопки на новый дизайн"

# Breaking change
git commit -m "feat(api): изменен формат ответа API

BREAKING CHANGE: поле 'userId' переименовано в 'id'"
```

### Правила
- ✅ Используйте повелительное наклонение ("добавить", а не "добавлен")
- ✅ Первая строка до 72 символов
- ✅ Подробное описание в body (если нужно)
- ✅ Ссылайтесь на issues: `Closes #123`
- ❌ Не используйте точку в конце subject

---

## 🔍 Pull Request процесс

### Перед созданием PR

1. ✅ Убедитесь, что код работает
2. ✅ Все тесты проходят (`npm test`)
3. ✅ Код отформатирован (`npm run format`)
4. ✅ Нет ESLint ошибок (`npm run lint`)
5. ✅ Обновлена документация (если нужно)
6. ✅ Добавлены тесты для новой функции

### Создание PR

**Заголовок:**
```
feat: краткое описание изменений
```

**Описание шаблона:**
```markdown
## Описание
Что изменено и почему?

## Тип изменения
- [ ] 🐛 Исправление бага
- [ ] ✨ Новая функция
- [ ] 📝 Обновление документации
- [ ] ♻️ Рефакторинг
- [ ] ⚡ Улучшение производительности

## Тестирование
Как это было протестировано?

## Скриншоты (если применимо)
![screenshot](url)

## Чеклист
- [ ] Код следует стандартам проекта
- [ ] Код прокомментирован
- [ ] Обновлена документация
- [ ] Изменения не создают warnings
- [ ] Добавлены тесты
- [ ] Все тесты проходят
```

### Review процесс

1. 👀 Maintainer проверит ваш PR
2. 💬 Могут быть запрошены изменения
3. ✅ После одобрения PR будет смержен
4. 🎉 Спасибо за вклад!

---

## 🐛 Отчеты об ошибках

### Перед созданием issue

1. 🔍 Проверьте, нет ли уже такого issue
2. 📝 Убедитесь, что это действительно баг
3. 🔄 Попробуйте воспроизвести на последней версии

### Шаблон отчета

```markdown
**Описание бага**
Четкое описание что не работает.

**Шаги воспроизведения**
1. Перейдите на '...'
2. Нажмите на '...'
3. Прокрутите до '...'
4. Увидите ошибку

**Ожидаемое поведение**
Что должно было произойти.

**Скриншоты**
Если применимо, добавьте скриншоты.

**Окружение:**
 - OS: [e.g. Windows 11]
 - Browser: [e.g. Chrome 120]
 - Version: [e.g. 1.0.0]

**Дополнительная информация**
Любая другая полезная информация.
```

---

## 💡 Предложения функций

### Перед созданием issue

1. 🔍 Проверьте roadmap проекта
2. 🔍 Убедитесь, что функция еще не предложена
3. 💭 Обдумайте, как это впишется в проект

### Шаблон предложения

```markdown
**Что это за функция?**
Краткое описание функции.

**Проблема которую она решает**
Какую проблему пользователей это решит?

**Предлагаемое решение**
Как вы видите реализацию?

**Альтернативы**
Какие альтернативные решения вы рассматривали?

**Дополнительная информация**
Mockup'ы, примеры, ссылки и т.д.
```

---

## 🧪 Тестирование

### Запуск тестов
```bash
# Все тесты
npm test

# С покрытием
npm run test:coverage

# Watch mode
npm run test:watch
```

### Написание тестов
```typescript
import { render, screen } from '@testing-library/react';
import { Button } from './Button';

describe('Button', () => {
  it('renders button with label', () => {
    render(<Button label="Click me" onClick={() => {}} />);
    expect(screen.getByText('Click me')).toBeInTheDocument();
  });

  it('calls onClick when clicked', () => {
    const handleClick = jest.fn();
    render(<Button label="Click" onClick={handleClick} />);
    screen.getByText('Click').click();
    expect(handleClick).toHaveBeenCalledTimes(1);
  });
});
```

---

## 📚 Дополнительные ресурсы

- [React Документация](https://react.dev/)
- [TypeScript Документация](https://www.typescriptlang.org/docs/)
- [Tailwind CSS Документация](https://tailwindcss.com/docs)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Semantic Versioning](https://semver.org/)

---

## ❓ Вопросы?

Не стесняйтесь задавать вопросы:

- 💬 [GitHub Discussions](https://github.com/your-username/utility-management-system/discussions)
- 📧 Email: contact@example.com
- 🐦 Twitter: [@yourhandle](https://twitter.com/yourhandle)

---

## 🙏 Спасибо!

Спасибо за то, что вносите вклад в проект! Каждый PR делает проект лучше. 🎉

---

<div align="center">

**Сделано с ❤️ для сообщества**

[⬆ Наверх](#-руководство-по-внесению-вклада)

</div>

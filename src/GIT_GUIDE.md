# 📘 Git & GitHub - Руководство по выгрузке проекта

Пошаговая инструкция для загрузки проекта на GitHub.

---

## 🎯 Что вам понадобится

- ✅ Git установлен на компьютере ([Скачать Git](https://git-scm.com/))
- ✅ Аккаунт на GitHub ([Регистрация](https://github.com/signup))
- ✅ Готовый проект с файлами

---

## 📋 Быстрая выгрузка (5 минут)

### Вариант 1: Через командную строку

#### 1️⃣ Проверьте Git
```bash
git --version
# Должно показать: git version 2.x.x
```

#### 2️⃣ Настройте Git (только первый раз)
```bash
# Укажите ваше имя
git config --global user.name "Ваше Имя"

# Укажите ваш email с GitHub
git config --global user.email "your-email@example.com"

# Проверьте настройки
git config --list
```

#### 3️⃣ Создайте репозиторий на GitHub
1. Зайдите на [github.com](https://github.com)
2. Нажмите кнопку **"New"** (зеленая кнопка)
3. Введите название: `utility-management-system`
4. Описание: `Система управления коммунальными услугами`
5. **НЕ** ставьте галочки "Add README" / "Add .gitignore" / "Choose a license"
6. Нажмите **"Create repository"**

#### 4️⃣ Инициализируйте Git в проекте
```bash
# Перейдите в папку проекта
cd путь/к/вашему/проекту

# Инициализируйте Git
git init

# Проверьте статус
git status
```

#### 5️⃣ Добавьте файлы
```bash
# Добавить все файлы
git add .

# Или добавить конкретные файлы
git add README.md SECURITY.md

# Проверьте что добавлено
git status
```

#### 6️⃣ Сделайте первый commit
```bash
git commit -m "Initial commit: Добавлена система управления коммунальными услугами"
```

#### 7️⃣ Создайте ветку main
```bash
git branch -M main
```

#### 8️⃣ Подключите GitHub репозиторий
```bash
# ЗАМЕНИТЕ на ваш URL с GitHub
git remote add origin https://github.com/ВАШ_USERNAME/utility-management-system.git

# Проверьте подключение
git remote -v
```

#### 9️⃣ Загрузите код на GitHub
```bash
git push -u origin main
```

#### 🎉 Готово!
Откройте ваш репозиторий: `https://github.com/ВАШ_USERNAME/utility-management-system`

---

### Вариант 2: Через GitHub Desktop (проще)

#### 1️⃣ Установите GitHub Desktop
Скачайте: [desktop.github.com](https://desktop.github.com/)

#### 2️⃣ Войдите в аккаунт
File → Options → Sign in to GitHub.com

#### 3️⃣ Добавьте проект
1. File → Add Local Repository
2. Выберите папку проекта
3. Нажмите "Create a repository" если нужно

#### 4️⃣ Сделайте commit
1. Введите Summary: "Initial commit"
2. Нажмите "Commit to main"

#### 5️⃣ Опубликуйте
1. Нажмите "Publish repository"
2. Название: `utility-management-system`
3. Снимите галочку "Keep this code private" (или оставьте для приватного)
4. Нажмите "Publish repository"

#### 🎉 Готово!

---

## 🔐 Настройка приватного репозитория

Если хотите сделать репозиторий приватным:

### На GitHub:
1. Settings → Danger Zone
2. Change repository visibility
3. Change to private

### При создании:
Поставьте галочку "Private" при создании репозитория

---

## 📝 Важные команды Git

### Проверка статуса
```bash
git status                  # Текущее состояние
git log                     # История коммитов
git log --oneline          # Краткая история
```

### Добавление файлов
```bash
git add .                   # Все файлы
git add file.txt           # Конкретный файл
git add *.ts               # Все .ts файлы
git add src/               # Вся папка
```

### Коммиты
```bash
git commit -m "Сообщение"                    # Коммит с сообщением
git commit -m "feat: новая функция"          # Conventional commit
git commit --amend -m "Исправленное"         # Изменить последний коммит
```

### Отправка на GitHub
```bash
git push                    # Отправить изменения
git push origin main       # Отправить в ветку main
git push -f                # Принудительная отправка (осторожно!)
```

### Получение изменений
```bash
git pull                    # Получить изменения с GitHub
git pull origin main       # Из конкретной ветки
git fetch                  # Загрузить без слияния
```

### Ветки
```bash
git branch                  # Список веток
git branch new-feature     # Создать ветку
git checkout new-feature   # Переключиться на ветку
git checkout -b feature    # Создать и переключиться
git merge feature          # Слить ветку в текущую
git branch -d feature      # Удалить ветку
```

### Отмена изменений
```bash
git restore file.txt       # Отменить изменения в файле
git restore .              # Отменить все изменения
git reset HEAD~1           # Отменить последний коммит
git reset --hard HEAD~1    # Удалить последний коммит (осторожно!)
```

### Другое
```bash
git clone URL              # Клонировать репозиторий
git remote -v              # Показать удаленные репозитории
git remote add name URL    # Добавить удаленный репозиторий
git stash                  # Спрятать изменения
git stash pop              # Вернуть спрятанное
```

---

## 🚨 Частые проблемы и решения

### ❌ "git: command not found"
**Решение:** Установите Git с [git-scm.com](https://git-scm.com/)

### ❌ "fatal: not a git repository"
**Решение:** 
```bash
git init
```

### ❌ "Permission denied (publickey)"
**Решение:**
1. Используйте HTTPS вместо SSH
2. Или настройте SSH ключ: [docs.github.com/ssh](https://docs.github.com/en/authentication)

### ❌ "refusing to merge unrelated histories"
**Решение:**
```bash
git pull origin main --allow-unrelated-histories
```

### ❌ "Your branch is behind 'origin/main'"
**Решение:**
```bash
git pull
# Разрешите конфликты если есть
git push
```

### ❌ Конфликты при слиянии
**Решение:**
1. Откройте файлы с конфликтами
2. Найдите маркеры `<<<<<<`, `======`, `>>>>>>`
3. Выберите нужную версию кода
4. Удалите маркеры
5. Сохраните файлы
```bash
git add .
git commit -m "Разрешены конфликты"
git push
```

### ❌ Случайно закоммитил большой файл
**Решение:**
```bash
# Удалить из последнего коммита
git rm --cached large-file.zip
git commit --amend -m "Удален большой файл"
git push -f

# Или использовать BFG Repo-Cleaner
```

### ❌ Нужно отменить push
**Решение:**
```bash
# Осторожно! Изменит историю!
git reset --hard HEAD~1
git push -f origin main
```

---

## 📋 Checklist перед первым push

- [ ] ✅ Проверил, что в репозитории нет `.env` файла
- [ ] ✅ Добавил `.gitignore` (должен быть в проекте)
- [ ] ✅ Проверил, что нет больших файлов (>100MB)
- [ ] ✅ Проверил, что нет секретов/паролей в коде
- [ ] ✅ Обновил README.md с правильными ссылками
- [ ] ✅ Написал понятное описание репозитория
- [ ] ✅ Добавил лицензию (LICENSE)
- [ ] ✅ Проверил, что код компилируется: `npm run build`
- [ ] ✅ Проверил, что тесты проходят: `npm test`

---

## 🎓 Workflow для разработки

### Базовый workflow
```bash
# 1. Получить последние изменения
git pull

# 2. Работать над кодом
# ... редактируете файлы ...

# 3. Проверить изменения
git status
git diff

# 4. Добавить изменения
git add .

# 5. Сделать коммит
git commit -m "feat: добавлена новая функция"

# 6. Отправить на GitHub
git push
```

### Feature Branch workflow
```bash
# 1. Создать ветку для новой функции
git checkout -b feature/new-payment-method

# 2. Работать над функцией
# ... редактируете файлы ...

# 3. Коммиты
git add .
git commit -m "feat: добавлен новый метод оплаты"

# 4. Отправить ветку на GitHub
git push origin feature/new-payment-method

# 5. Создать Pull Request на GitHub

# 6. После одобрения - слить в main
git checkout main
git merge feature/new-payment-method
git push

# 7. Удалить ветку
git branch -d feature/new-payment-method
git push origin --delete feature/new-payment-method
```

---

## 📚 Полезные ресурсы

### Официальная документация
- 📖 [Git Book (RU)](https://git-scm.com/book/ru/v2)
- 📖 [GitHub Docs](https://docs.github.com/)
- 📖 [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)

### Интерактивные туториалы
- 🎮 [Learn Git Branching](https://learngitbranching.js.org/?locale=ru_RU)
- 🎮 [Git-it](https://github.com/jlord/git-it-electron)
- 📺 [Git и GitHub Курс [RU]](https://www.youtube.com/watch?v=zZBiln_2FhM)

### Инструменты
- 🖥️ [GitHub Desktop](https://desktop.github.com/)
- 🖥️ [SourceTree](https://www.sourcetreeapp.com/)
- 🖥️ [GitKraken](https://www.gitkraken.com/)
- 🔌 [VS Code Git Extension](https://code.visualstudio.com/docs/sourcecontrol/overview)

### Шпаргалки
- 📄 [Git Cheat Sheet RU](https://training.github.com/downloads/ru/github-git-cheat-sheet/)
- 📄 [Conventional Commits](https://www.conventionalcommits.org/)

---

## 🎯 Следующие шаги после загрузки

1. ⚙️ **Настройте GitHub Actions** для CI/CD
2. 🔒 **Добавьте секреты** в Settings → Secrets
3. 📝 **Создайте Wiki** для дополнительной документации
4. 🏷️ **Добавьте темы** (topics) для лучшей находимости
5. 🎨 **Настройте README badges** с shields.io
6. 📊 **Настройте Projects** для управления задачами
7. 💬 **Включите Discussions** для сообщества
8. 🔐 **Настройте branch protection** для main ветки

---

## 🆘 Нужна помощь?

- 💬 [GitHub Community](https://github.com/community)
- 📧 Email: contact@example.com
- 🐦 Twitter: [@yourhandle](https://twitter.com/yourhandle)

---

<div align="center">

**Успешной работы с Git! 🚀**

[⬆ Наверх](#-git--github---руководство-по-выгрузке-проекта)

</div>
